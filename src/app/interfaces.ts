export interface User {
  userId: number;
  userName: string;
}

export interface Project {
  projectId: number;
  projectName: string;
  projectSize: string;
  projectManagerId: number;
  projectManagerName: string;
}
